<%_
  var data = {
    url: '',/* スラッシュ始まりのドメイン以下URL ( TOPの場合は空文字で ) */
    name: 'top',/* bodyタグにつくid名 */
    title:'',/* TOPの場合は空文字で */
    description:'',/* TOPの場合は空文字で */
    keyword:'',/* TOPの場合は空文字で */
    device: 'pc', 
    current:'./'   
  } 
_%>  
<%_ data.asset = function(){ _%> 

<%_ }; data.content = function(){ _%>
<main>
  <div>
    <dl>
      <div>
        <dt>userAgentData取れているかどうか</dt>
        <dd id="text"></dd>
      </div>
      <div>
        <dt>navigator.userAgent</dt>
        <dd id="uatext"></dd>
      </div>
      <div>
        <dt>navigator.userAgentData</dt>
        <dd id="uadtext"></dd>
      </div>
      <div>
        <dt>navigator.userAgentData.platform</dt>
        <dd id="uadtext1"></dd>
      </div>
      <div>
        <dt>navigator.userAgentData.brands</dt>
        <dd id="uadtext2"></dd>
      </div>
      <div>
        <dt>navigator.userAgentData.mobile</dt>
        <dd id="uadtext3"></dd>
      </div>
    </dl>
  </div>
</main>
<%_ } _%> 
<%_ /* 以下includeファイル */ _%>
<%- include( data.current + 'includes/_head_' + data.device , { data : data } ); %>
<%- data.content(); %>
<%- include( data.current + 'includes/_footer_' + data.device , { data : data } ); %>
<%- data.asset(); %>
<%- include( data.current + 'includes/_foot_' + data.device , { data : data } ); %>
