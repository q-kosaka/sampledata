<%_
  var data = {
    url: '',/* スラッシュ始まりのドメイン以下URL ( TOPの場合は空文字で ) */
    name: 'canvas',/* bodyタグにつくid名 */
    title:'',/* TOPの場合は空文字で */
    description:'',/* TOPの場合は空文字で */
    keyword:'',/* TOPの場合は空文字で */
    device: 'pc', 
    current:'./'   
  } 
_%>  
<%_ data.asset = function(){ _%> 

<%_ }; data.content = function(){ _%>
<main>
  <canvas id="imageBox" width="1200px" height="1800px"></canvas>

  <div><a id="makeimage">画像作成</a></div>

  <div></div>
  <div>
    <p>dataURL（Chrome系+スマホのブラウザだと画像パス開けない）</p>
    <p><a id="download">画像ダウンロード</a></p>
    <p></p><a id="download_dataURL" target="_blank">画像パス</a></p>
  </div>
  <div>
    <p>blob（Chrome系+スマホのブラウザでも画像パス開ける）</p>
    <p><a id="download_blob">画像ダウンロード</a></p>
    <p><a id="download_blobURL" target="_blank">画像パス</a></p>
  </div>
</main>
<%_ } _%> 
<%_ /* 以下includeファイル */ _%>
<%- include( data.current + 'includes/_head_' + data.device , { data : data } ); %>
<%- data.content(); %>
<%- include( data.current + 'includes/_footer_' + data.device , { data : data } ); %>
<%- data.asset(); %>
<%- include( data.current + 'includes/_foot_' + data.device , { data : data } ); %>
